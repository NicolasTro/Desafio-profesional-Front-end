import Button from "@/Components/Button";






export default function Dashboard() {
  return (
    <div>
      <h1>Dashboard</h1>

      <Button label="Logout" onClick={() => {}} />
    </div>
  );
}
